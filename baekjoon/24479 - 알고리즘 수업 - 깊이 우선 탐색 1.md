https://www.acmicpc.net/problem/24479

노드를 넣을때 소팅하면 괜히 시간이 오래걸린다. 나중에 실제로 인접리스트를 가져올때 소팅하자.
소팅은 버블소트쓰면 시간복잡도가 $O(n^2)$ 이라 별로니 머지소트를 쓰자. linked list merge sort는 [여기](https://www.geeksforgeeks.org/merge-sort-for-linked-list/)를 참고했다.

인접리스트에 insert할때도 list의 끝을 알고있으면 좋아서 `tails`변수를 따로 도입함.
malloc이 느릴까봐 노드풀에서 노드 할당하는 친구를 만들어놓긴했는데, malloc도 내부에 spatial locality를 구현했을거라 큰 차이는 없을듯..

```
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define N 100000

#define LOG 0
#define write_log(format,args...)                 \
                  if (LOG) {                      \
                      printf(format, ## args);    \
                  }

int visited[N+1];
int order = 1;

typedef struct node Node;
struct node {
    int n;
    Node* next;
};

typedef struct graph Graph;
struct graph {
    int size; // # of nodes
    Node *adj[N+1];
    Node *tails[N+1];
} g;

void print_graph() {
#if LOG
    for (int i=1; i<=g.size; i++) {
        if (g.adj[i]) {
            printf("adj[%d] = ", i);
            Node *n = g.adj[i];
            while (n) {
                printf("%d, ", n->n);
                n = n->next;
            }
            printf("\n");
        }
    }
#endif
}

Node node_pool[N*5];
int pc;

Node *get_new_node() {
    return &node_pool[pc++];
}

void add_edge(int u, int v) {
    Node *node = get_new_node();
    node->next = NULL;
    node->n = v;

    if (!g.adj[u]) {
        g.adj[u] = node;
    } else {
        g.tails[u]->next = node;
    }
    g.tails[u] = node;
}

void split(Node *source, Node **frontRef, Node **backRef) {
    Node *fast;
    Node *slow;

    slow = source;
    fast = source->next;

    while (fast != NULL) {
        fast = fast->next;
        if (fast != NULL) {
            slow = slow->next;
            fast = fast->next;
        }
    }

    *frontRef = source;
    *backRef = slow->next;
    slow->next = NULL;
}

Node *merge(Node *a, Node *b) {
    Node *result = NULL;
    if (a == NULL) { return b; }
    if (b == NULL) { return a; }

    if (a->n <= b->n) {
        result = a;
        result->next = merge(a->next, b);
    } else {
        result = b;
        result->next = merge(a, b->next);
    }

    return result;
}

void mergeSort(Node **headRef) {
    Node *head = *headRef;
    Node *a;
    Node *b;

    if (head == NULL || head->next == NULL) {
        return;
    }

    split(head, &a, &b);
    mergeSort(&a);
    mergeSort(&b);

    *headRef = merge(a, b);
}

void dfs(int r) {
    visited[r] = order++;
    write_log("visited %d\n", r);
    Node *node = g.adj[r];

    if (node) {
        Node *adjlist = node;
        mergeSort(&adjlist);
        g.adj[r] = adjlist;
        node = g.adj[r];
    }

    while (node != NULL) {
        if (visited[node->n] == 0) {
            dfs(node->n);
        }
        node = node->next;
    }
}

int main() {
    int n, m, r, i;
    scanf("%d%d%d", &n, &m, &r);

    g.size = n;

    for (i=0; i<m; i++) {
        int u,v;
        scanf("%d%d", &u, &v);

        add_edge(u, v);
        add_edge(v, u);
    }

    print_graph();

    dfs(r);

    for (i=1; i<=n; i++) {
        printf("%d\n", visited[i]);
    }

    return 0;
}

```